# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups

create table "GAMES" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"GAME_SERIES_ID" BIGINT NOT NULL,"HOSTED" VARCHAR NOT NULL,"DATE" TIMESTAMP NOT NULL,"STAKE" DOUBLE NOT NULL,"CURRENCY" VARCHAR NOT NULL);
create table "GAME_RESULTS" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"GAME_ID" BIGINT NOT NULL,"PLAYER_ID" BIGINT NOT NULL,"POSITION" INTEGER NOT NULL,"STAKE" DOUBLE,"WINNINGS" DOUBLE,"CURRENCY" VARCHAR);
create table "GAME_SERIES" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL);
create table "PLAYERS" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL,"NICKNAMES" VARCHAR NOT NULL,"AUTH_ID" VARCHAR,"AUTH_TOKEN" BLOB,"AUTH_TYPE" INTEGER);
create table "SKILL_ENTRIES" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"GAME_RESULT_ID" BIGINT NOT NULL,"MEAN" DOUBLE NOT NULL,"STDDEV" DOUBLE NOT NULL);

# --- !Downs

drop table "GAMES";
drop table "GAME_RESULTS";
drop table "GAME_SERIES";
drop table "PLAYERS";
drop table "SKILL_ENTRIES";

